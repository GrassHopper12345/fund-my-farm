<!--Main section of the homepage-->
<div class="#">
    {{#each farms as |farm| }}

    <div class="row">
        <div class="col s12 m6">
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <span class="card-title"><a href="/farm/{{farm.id}}">{{farm.farm_name}}</a></span>
                    <p>{{farm.description}}</p>
                </div>
                <div class="card-action">
                    <a href="/farm/{{farm.id}}">Farm</a>
                    <a href="#">Investment</a>
                </div>
            </div>
        </div>
    </div>
    {{!-- <h2>
        <a href="/farm/{{farm.id}}">{{farm.farm_name}}</a>
    </h2>
    <p>
        {{farm.description}}
    </p> --}}
    {{/each}}
</div>
<!--Sidebar if we can-->
<div>

</div>