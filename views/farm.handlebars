<div class="text-center">
  <h2>Welcome to {{name}}</h2>
  
  <p>{{description}}</p>

  <p>
    {{#if farm.product === produce}}
    {{{get_crop}}}
    {{else}}
    {{{get_livestock}}}
    We carry {{product.product_name}}
  </p>
  <p>Currently priced at {{product.price}}</p>

  <p>We have {{product.stock}} in stock</p>

  <p>
    <button class="btn"> Buy Now! </button>
  </p>
</div>